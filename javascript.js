// var order = ["morango", "banana", "manga", "uva", "mamão"];
// order.pop(); (remover o ultimo elemento)

// order.push("limão") (adicionar um elemento no final)

// order.shift(); (remover o primeiro elemento)

// order.unshift("pêra"); (adicionar um elemento no começo)

// order [2] = "abacaxi" ; (substituir um item do array -o primeiro elemento do array conta como 0-)

// order[order.length] = caqui; (acessar o tamanho do array e adiconar um item no final)

// delete order[4]; (declarar um item como indefinido)

// order.splice(2,1,"kiwi", "jaca"); (primeiro:posição ; segundo: quantos vão ser substituidos; terceiro, quarto, etc:o que vai substituir aquele item)

// order.splice(0,2); (deleta algum item. primeiro:posição; segundo:quantida de items à serem deletados)

// console.log(order);


var girls = ["Ana", "Gabi", "Bia", "Mari"];
var boys = ["João", "Leo", "Caio", "Jonas"];
var teacher = ["clarice", "thais", "anderson", "alan"];

var people = girls.concat(boys);
// console.log(people);

// var everyone = girls.concat (boys, teacher);
// console.log(everyone); 


// document.getElementById("demo") (chamar o id do index)
document.querySelector("#demo").innerHTML = people;

// people.sort(); (organizar em ordem crescente)
for (let index = 0; index < array.length; index++) {
    if (condition) {

    } else {

    }
}